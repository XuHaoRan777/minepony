<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.MineponyPromotionalImageMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.ruoyi.system.domain.MineponyPromotionalImage">
        <id column="id" property="id" />
        <result column="enable" property="enable" />
        <result column="imagename" property="imagename" />
        <result column="savepath" property="savepath" />
        <result column="uploaddate" property="uploaddate" />
        <result column="uploaderid" property="uploaderid" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, enable, imagename, savepath, uploaddate, uploaderid
    </sql>

</mapper>
